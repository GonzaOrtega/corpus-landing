/**
 * Adapters for the external-api capability are constructed here, and nowhere else — see
 * construction.adapters-only-in-capabilities.
 */
export type ExternalApiDeps = {};

export const provideExternalApi = (): ExternalApiDeps => ({});
