Filed by review-gate from an AI reviewer finding. Not human-verified.

### R-03
- file: `src/config/sentry-options.ts:183`
- severity: minor · category: security · scope: in · threat: in
- sources: pr-review-toolkit:code-reviewer
- summary: Three cleaning steps are installed for errors, traces and log lines, but not for the standalone performance measurements the browser sends on their own, so if browser performance tracking is ever switched on those would leave uncleaned.
- reason deferred: Below the severity floor (important)

### R-05
- file: `src/config/maintenance-schedule.ts:4`
- severity: minor · category: docs · scope: in · threat: in
- sources: pr-review-toolkit:code-reviewer
- summary: A comment tells the next reader that a particular test guards the nightly job's schedule, but no test by that name exists — the real check lives somewhere else.
- reason deferred: Below the severity floor (important)

### R-07
- file: `instrumentation-client.ts:40`
- severity: minor · category: correctness · scope: in · threat: in
- sources: pr-review-toolkit:silent-failure-hunter
- summary: Browser error reporting is started from an idle moment with no deadline and is never retried, so in a background tab, or after a single failed download, the page reports nothing for the rest of its life and nothing says so.
- reason deferred: Below the severity floor (important)

### R-08
- file: `src/features/early-access/actions/join-early-access.action.ts:28`
- severity: minor · category: correctness · scope: in · threat: in
- sources: pr-review-toolkit:silent-failure-hunter
- summary: The new tracing wrapper, and the request-header lookup it needs, sit outside the signup action's own safety net, so a failure inside the monitoring code now breaks the form instead of showing the visitor the polite "please try again" message.
- reason deferred: Below the severity floor (important)

### R-09
- file: `src/adapters/observability/noop-error-reporter.adapter.ts:10`
- severity: minor · category: correctness · scope: in · threat: in
- sources: pr-review-toolkit:silent-failure-hunter
- summary: Wherever no reporting address is configured - every developer machine, and any preview not yet set up - the failure is handed to an empty function, so it still vanishes with no log line anywhere.
- reason deferred: Below the severity floor (important)

### R-10
- file: `src/config/sentry-options.ts:129`
- severity: minor · category: security · scope: in · threat: in
- sources: pr-review-toolkit:silent-failure-hunter, security
- summary: The design says fields picked up from the application log are re-checked against the short list of fields allowed to leave the machine; for log records they are only checked against a list of forbidden names, so any other field a logger attaches still goes out.
- reason deferred: Below the severity floor (important)

### R-11
- file: `src/composition/capabilities/observability.ts:24`
- severity: minor · category: correctness · scope: in · threat: in
- sources: pr-review-toolkit:silent-failure-hunter
- summary: Whether boundary failures get reported is decided when the site is built, while the reporting library itself decides when the site runs, so the two can disagree and failures can be dropped silently while everything else is reported normally.
- reason deferred: Below the severity floor (important)

### R-17
- file: `app/error.tsx:20`
- severity: minor · category: test-gap · scope: in · threat: in
- sources: pr-review-toolkit:pr-test-analyzer
- summary: The two screens that catch failures happening inside the visitor's browser, and the loader that starts the browser reporter after the page settles, have no tests at all.
- reason deferred: Below the severity floor (important)

### R-18
- file: `proxy.ts:144`
- severity: minor · category: test-gap · scope: in · threat: in
- sources: pr-review-toolkit:pr-test-analyzer
- summary: The address the browser sends its reports to is written out by hand in two places, and unlike the cron schedule this PR does pin, nothing checks the two stay the same.
- reason deferred: Below the severity floor (important)

### R-19
- file: `src/config/maintenance-schedule.ts:3`
- severity: minor · category: docs · scope: in · threat: in
- sources: pr-review-toolkit:pr-test-analyzer
- summary: A comment sends the next maintainer to a test file that does not exist; the real check lives somewhere else.
- reason deferred: Below the severity floor (important)

### R-20
- file: `src/config/sentry-options.ts:92`
- severity: minor · category: test-gap · scope: in · threat: in
- sources: pr-review-toolkit:pr-test-analyzer
- summary: The scrubber stops cleaning once a payload nests more than twelve levels deep and hands the rest through untouched, and no test pins that boundary.
- reason deferred: Below the severity floor (important)

### R-21
- file: `src/features/early-access/actions/management.actions.test.ts:56`
- severity: minor · category: test-gap · scope: in · threat: in
- sources: pr-review-toolkit:pr-test-analyzer
- summary: A check meant to prove the secret link never reaches the reporter is written so that it can never fail, which makes the coverage look stronger than it is.
- reason deferred: Below the severity floor (important)

### R-22
- file: `src/features/early-access/actions/unsubscribe.action.ts:16`
- severity: minor · category: test-gap · scope: in · threat: in
- sources: pr-review-toolkit:pr-test-analyzer
- summary: The signup form has a test proving that a broken configuration is still reported before the visitor sees the polite retry message; the two management actions have the same code path with no such test.
- reason deferred: Below the severity floor (important)

### R-24
- file: `src/config/sentry-options.ts:74`
- severity: minor · category: security · scope: in · threat: in
- sources: security
- summary: The rule that blanks out credential-carrying web-address parameters only fires when the parameter is preceded by '?' or '&', which is never true for the very field the scrubber goes out of its way to keep.
- reason deferred: Below the severity floor (important)

### R-25
- file: `src/config/sentry-options.ts:80`
- severity: minor · category: docs · scope: in · threat: in
- sources: security
- summary: The claim that the database address is stripped before anything leaves the machine is stronger than what the code does: only the username and password are removed, and the database host still travels on every outgoing-call trace.
- reason deferred: Below the severity floor (important)

### R-26
- file: `src/config/sentry-options.test.ts:67`
- severity: minor · category: test-gap · scope: in · threat: in
- sources: security
- summary: No test feeds the scrubber an event shaped like one from a real browser on the management page, so the case where the secret link sits in the address is never checked.
- reason deferred: Below the severity floor (important)

### R-27
- file: `lighthouserc.cjs:15`
- severity: minor · category: docs · scope: out · threat: in
- sources: pr-review-toolkit:code-reviewer
- summary: A comment in the performance-test config promises that a local run sends nothing to the real error-tracking account, but only the server half of the run is actually silenced — the browser half can still send, as the runbook edited in the very same change admits.
- reason deferred: Outside the gate's scope

### R-28
- file: `docs/operations/sentry.md:53`
- severity: minor · category: docs · scope: in · threat: in
- sources: pr-review-toolkit:code-reviewer
- summary: The runbook tells maintainers that the unit-test command is kept away from the real error tracker by blanking configuration values, but that command runs no build and blanks nothing — a different safeguard is what actually protects it.
- reason deferred: Below the severity floor (important)

### R-30
- file: `src/config/sentry-options.ts:320`
- severity: minor · category: correctness · scope: in · threat: in
- sources: pr-review-toolkit:silent-failure-hunter
- summary: A mistyped or unsupported Sentry address quietly turns all browser error reporting off instead of complaining, so a configuration mistake looks exactly like Sentry never having been set up.
- reason deferred: Below the severity floor (important)

### R-31
- file: `src/config/sentry-client.ts:42`
- severity: minor · category: correctness · scope: in · threat: in
- sources: pr-review-toolkit:silent-failure-hunter
- summary: The one-shot flag that stops the error reporter starting twice is raised before the start-up actually succeeds, so a failed start-up permanently disables reporting while every later attempt reports success.
- reason deferred: Below the severity floor (important)

### R-32
- file: `src/config/sentry-options.ts:160`
- severity: minor · category: other · scope: in · threat: in
- sources: pr-review-toolkit:silent-failure-hunter
- summary: The new rule that strips the secret part of management links also blanks the name of whichever button or field a visitor last touched, removing the main clue for diagnosing a broken page.
- reason deferred: Below the severity floor (important)

### R-36
- file: `app/error.test.tsx:9`
- severity: minor · category: test-gap · scope: in · threat: in
- sources: pr-review-toolkit:pr-test-analyzer
- summary: The two new error-page tests cover the reporting helper but never the page that is supposed to call it, so deleting the single line that fires the report when the error page appears would silently end all browser error reporting with the tests still green.
- reason deferred: Below the severity floor (important)

### R-37
- file: `app/monitoring/monitoring-route.handler.test.ts:61`
- severity: minor · category: test-gap · scope: in · threat: in
- sources: pr-review-toolkit:pr-test-analyzer
- summary: A size-limit test is named for a request that understates how big it is, but the request it builds declares no size at all, so the case a reader would assume is pinned is not the case that runs.
- reason deferred: Below the severity floor (important)

### R-38
- file: `src/core/testing/never-log-fixture.ts:25`
- severity: minor · category: test-gap · scope: in · threat: in
- sources: pr-review-toolkit:pr-test-analyzer
- summary: The shared list of forbidden sample values is maintained by hand next to the object it is derived from, so adding a new forbidden field to the object without also adding it to the list would quietly shrink what both test suites check.
- reason deferred: Below the severity floor (important)

### R-39
- file: `tests/unit/e2e-runtime-config.test.ts:53`
- severity: minor · category: test-gap · scope: in · threat: in
- sources: pr-review-toolkit:pr-test-analyzer
- summary: The pipeline marker that keeps a local Lighthouse run from behaving like a real deployment was added to the config but is asserted nowhere, so removing it would go unnoticed.
- reason deferred: Below the severity floor (important)

### R-41
- file: `src/config/sentry-options.ts:164`
- severity: minor · category: performance · scope: in · threat: in
- sources: security
- summary: One of the four text-cleaning rules gets dramatically slower the longer a piece of text is, so an unusually long line inside an error report can freeze the request that is trying to send it.
- reason deferred: Below the severity floor (important)

### R-42
- file: `src/config/sentry-options.ts:160`
- severity: minor · category: correctness · scope: in · threat: in
- sources: security
- summary: The new rule that blanks out anything after a '#' is much broader than the comment admits: it also destroys the element identifiers in click history, framework error numbers and ordinary text, which is exactly the detail an engineer needs when reading a report.
- reason deferred: Below the severity floor (important)

### R-43
- file: `src/config/sentry-options.ts:202`
- severity: minor · category: docs · scope: in · threat: in
- sources: security
- summary: A comment states that the page address field is the only place the subscriber's secret link can travel, which is not true — two other fields carry it as well, and those are protected only by the new text rule, not by the structural fix.
- reason deferred: Below the severity floor (important)

### R-44
- file: `src/config/sentry-options.test.ts:81`
- severity: minor · category: test-gap · scope: in · threat: in
- sources: security
- summary: The tests prove the secret link is stripped from the page-address field, but never prove it is stripped from the two other fields that carry it, which are the ones relying on a pattern that a future edit could weaken.
- reason deferred: Below the severity floor (important)

### R-45
- file: `src/config/sentry-options.ts:50`
- severity: minor · category: correctness · scope: in · threat: in
- sources: security
- summary: The check that recognises the error-tracking address only accepts a two-letter region code, so a legitimate address with a longer region name would silently switch off all browser error reporting with no warning anywhere.
- reason deferred: Below the severity floor (important)

### R-46
- file: `app/monitoring/monitoring-route.handler.ts:43`
- severity: minor · category: security · scope: in · threat: in
- sources: security
- summary: When a caller does not declare how big the upload is, the whole upload is pulled into memory before the size limit is applied, so the limit bounds what gets forwarded but not what gets held.
- reason deferred: Below the severity floor (important)


Complete ledger: `docs/quality/reviews/pr-19/ledger.yaml` in PR #19.

<!-- review-gate:published pr-19
R-03
R-05
R-07
R-08
R-09
R-10
R-11
R-17
R-18
R-19
R-20
R-21
R-22
R-24
R-25
R-26
R-27
R-28
R-30
R-31
R-32
R-36
R-37
R-38
R-39
R-41
R-42
R-43
R-44
R-45
R-46
-->
