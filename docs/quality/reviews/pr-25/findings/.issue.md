Filed by review-gate from an AI reviewer finding. Not human-verified.

### R-01
- file: `src/config/sentry-options.ts:268`
- severity: minor · category: test-gap · scope: in · threat: in
- sources: pr-review-toolkit:code-reviewer
- summary: The one line that cleans a web address the code cannot fully understand was changed but is never exercised by a test, so nobody would notice if it stopped cleaning.
- reason deferred: Below the severity floor (important)

### R-02
- file: `src/config/sentry-options.ts:209`
- severity: minor · category: security · scope: in · threat: in
- sources: pr-review-toolkit:code-reviewer, security
- summary: The rule that hides the part of a link after the hash was deliberately narrowed, and a few unusual spellings of that link now pass through uncleaned.
- reason deferred: Below the severity floor (important)

### R-03
- file: `next.config.ts:115`
- severity: minor · category: docs · scope: in · threat: in
- sources: pr-review-toolkit:code-reviewer
- summary: A new one-line call was inserted between an explanatory comment and the thing that comment describes, so the comment now reads as if it were about the new line.
- reason deferred: Below the severity floor (important)

### R-07
- file: `src/features/early-access/actions/instrumented-action.ts:34`
- severity: minor · category: correctness · scope: in · threat: in
- sources: pr-review-toolkit:silent-failure-hunter
- summary: Obtaining the error reporter happens outside the protective wrapper, so the one step that runs before any reporter exists can still crash the whole action instead of turning into the polite retry the file promises.
- reason deferred: Below the severity floor (important)

### R-08
- file: `src/adapters/observability/noop-error-reporter.adapter.ts:26`
- severity: minor · category: correctness · scope: in · threat: in
- sources: pr-review-toolkit:silent-failure-hunter
- summary: The stand-in reporter's whole purpose is to write one line instead of discarding the failure, but if that write fails it discards the failure anyway with an empty catch and no fallback.
- reason deferred: Below the severity floor (important)

### R-09
- file: `src/composition/capabilities/observability.ts:34`
- severity: minor · category: correctness · scope: in · threat: in
- sources: pr-review-toolkit:silent-failure-hunter
- summary: The comment says a boundary is never handed a reporter the reporting service will quietly drop, but the rule it uses only checks that an address was supplied, not that it is usable — so a typo leaves failures vanishing at runtime with no log line either.
- reason deferred: Below the severity floor (important)

### R-10
- file: `instrumentation-client.ts:76`
- severity: minor · category: docs · scope: in · threat: in
- sources: pr-review-toolkit:silent-failure-hunter
- summary: When browser reporting fails to start twice in a row, the only record is a line in that one visitor's own browser console, which no operator ever sees — so 'reporting was off for some visitors' stays invisible.
- reason deferred: Below the severity floor (important)

### R-11
- file: `src/core/testing/never-log-fixture.test.ts:19`
- severity: minor · category: test-gap · scope: in · threat: in
- sources: pr-review-toolkit:silent-failure-hunter, pr-review-toolkit:pr-test-analyzer
- summary: A new test checks that the list of values-never-to-log has no duplicates, but the list is built by de-duplicating, so that check can never fail and proves nothing.
- reason deferred: Below the severity floor (important)

### R-14
- file: `src/config/sentry-options.test.ts:322`
- severity: minor · category: test-gap · scope: in · threat: in
- sources: pr-review-toolkit:pr-test-analyzer
- summary: A test named for proving that missing fields stay missing cannot actually detect an invented field, because the comparison it uses treats a present-but-empty field as absent.
- reason deferred: Below the severity floor (important)

### R-15
- file: `app/error.test.tsx:48`
- severity: minor · category: test-gap · scope: in · threat: in
- sources: pr-review-toolkit:pr-test-analyzer
- summary: The new error-page tests replace the framework's after-render hook with one that ignores its dependency list, so a change that stops a second, different failure from being reported would still pass.
- reason deferred: Below the severity floor (important)

### R-16
- file: `next.config.ts:115`
- severity: minor · category: test-gap · scope: in · threat: in
- sources: pr-review-toolkit:pr-test-analyzer
- summary: The new build-time warning about a misspelt reporting address is tested on its own but nothing checks that the build configuration still calls it, so deleting the call breaks nothing.
- reason deferred: Below the severity floor (important)

### R-17
- file: `src/features/early-access/actions/instrumented-action.ts:51`
- severity: minor · category: test-gap · scope: in · threat: in
- sources: pr-review-toolkit:pr-test-analyzer
- summary: The new shared wrapper turns every failure into the polite retry answer, including the framework's own internal signals, and only a comment records that today's actions never raise one.
- reason deferred: Below the severity floor (important)

### R-19
- file: `src/core/testing/never-log-fixture.ts:12`
- severity: minor · category: test-gap · scope: in · threat: in
- sources: security
- summary: The shared list of values that must never leave the machine only contains short sample values, so nothing in the suite exercises the new length limits the scrubber now applies.
- reason deferred: Below the severity floor (important)

### R-20
- file: `app/monitoring/monitoring-route.handler.ts:82`
- severity: minor · category: correctness · scope: in · threat: in
- sources: security
- summary: The new chunk-by-chunk body reader in the forwarding endpoint has no failure path, so a caller that starts sending and then drops the connection produces a server error with no log line, although this handler promises that every fault is recorded.
- reason deferred: Below the severity floor (important)

### R-21
- file: `src/composition/capabilities/observability.ts:38`
- severity: minor · category: performance · scope: in · threat: in
- sources: security
- summary: Wherever error reporting is switched off, a fresh logger is now built from scratch every time a form submission or link lookup asks for a reporter.
- reason deferred: Below the severity floor (important)

### R-22
- file: `app/monitoring/route.ts:62`
- severity: minor · category: correctness · scope: in · threat: in
- sources: pr-review-toolkit:code-reviewer
- summary: The new "we are dropping error reports on purpose" warning line will also be written on a routine local speed-test run, even though the comment right above it promises the line only ever appears when a real deployment is misconfigured.
- reason deferred: Below the severity floor (important)

### R-24
- file: `app/monitoring/route.ts:63`
- severity: minor · category: correctness · scope: in · threat: in
- sources: pr-review-toolkit:silent-failure-hunter
- summary: A fact about how the deployment is configured is written to the log once for every single request, so one misconfiguration becomes an endless stream of identical lines that any visitor can keep producing.
- reason deferred: Below the severity floor (important)

### R-25
- file: `src/config/runtime-environment.ts:34`
- severity: minor · category: correctness · scope: in · threat: in
- sources: pr-review-toolkit:silent-failure-hunter
- summary: A pipeline that spells its marker out as 'false' or '0' is now treated as a real deployment, switching real mail sending and error reporting back on, and nothing anywhere records that a marker was seen and ignored.
- reason deferred: Below the severity floor (important)

### R-27
- file: `src/features/early-access/actions/instrumented-action.test.ts:111`
- severity: minor · category: test-gap · scope: in · threat: in
- sources: pr-review-toolkit:pr-test-analyzer
- summary: One of the two new tests added to prove the R-04 fix passes just as happily against the old, unfixed code, so it does not actually prove anything about that fix.
- reason deferred: Below the severity floor (important)

### R-28
- file: `app/monitoring/route.test.ts:120`
- severity: minor · category: test-gap · scope: in · threat: in
- sources: pr-review-toolkit:pr-test-analyzer
- summary: The new tests never try the one setup this project actually ships for its own test runs — a blank monitoring address plus the pipeline marker — so nothing stops a future change from writing a misleading warning line on every request of every local test and speed-audit run.
- reason deferred: Below the severity floor (important)

### R-29
- file: `src/config/runtime-environment.ts:37`
- severity: minor · category: test-gap · scope: in · threat: in
- sources: pr-review-toolkit:pr-test-analyzer
- summary: The new rule for deciding whether a pipeline marker means yes or no has no test of its own, and two parts of it can be deleted without any test noticing.
- reason deferred: Below the severity floor (important)

### R-30
- file: `app/monitoring/monitoring-route.handler.test.ts:197`
- severity: minor · category: test-gap · scope: in · threat: in
- sources: pr-review-toolkit:pr-test-analyzer
- summary: The new test's name and comment describe a caller that sends some data and then drops the connection, but the way the test builds that request means no data is ever delivered, so the half-sent case it advertises is not the case it runs.
- reason deferred: Below the severity floor (important)

### R-31
- file: `src/config/sentry-options.test.ts:392`
- severity: minor · category: test-gap · scope: in · threat: in
- sources: pr-review-toolkit:pr-test-analyzer
- summary: The new helper permanently reroutes every future log line produced anywhere in that test file into a throwaway stand-in, and the comment explaining why that is safe only addresses calling the helper twice.
- reason deferred: Below the severity floor (important)

### R-32
- file: `app/monitoring/route.ts:63`
- severity: minor · category: security · scope: in · threat: in
- sources: security
- summary: On a public deployment wrongly marked as a pipeline run, every anonymous request to the monitoring endpoint now writes a log line, so a flood of requests becomes a flood of logs.
- reason deferred: Below the severity floor (important)


Complete ledger: `docs/quality/reviews/pr-25/ledger.yaml` in PR #25.

<!-- review-gate:published pr-25
R-01
R-02
R-03
R-07
R-08
R-09
R-10
R-11
R-14
R-15
R-16
R-17
R-19
R-20
R-21
R-22
R-24
R-25
R-27
R-28
R-29
R-30
R-31
R-32
-->
